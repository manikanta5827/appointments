

export const UserRole = Object.freeze({
    STUDENT: "student",
    PROFESSOR: "professor",
  });
  
export const AppointmentStatus = Object.freeze({
    BOOKED: "booked",
    CANCELLED: "cancelled",
  });